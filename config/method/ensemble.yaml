# Ensemble Method Configuration
name: ensemble

classifier: logistic

params:
  mode: voting  # voting, stacking, concat
  methods:
    - lapeigvals
    - lookback_lens
    - entropy
  weights: null  # null for equal weights

cv_folds: 5
random_seed: 42
